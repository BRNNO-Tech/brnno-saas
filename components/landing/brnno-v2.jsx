"use client";

/**
 * Marketing landing entry. Re-exports the Next-ready implementation from
 * `marketing-landing.tsx` (waitlist API, /book-demo, footer links, etc.).
 * Replace this file with your standalone `brnno-v2.jsx` from the sandbox if you
 * prefer a single self-contained module — keep `"use client"` as the first line.
 */
export { default } from "./marketing-landing";
